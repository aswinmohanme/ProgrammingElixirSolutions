#
# Emacs has awesome elixir mode, 
# Get elixir help by SPC m h h

# Get help from the elixir iex
h(Enum.map)

# Get help about the IO.puts 
h IO.puts

# i shows help about value
i 123

# ex is for files who are compiled to bytecode and exs is for script files

# c is used to compiel inside iex, and import_file is used to load the code as it was written into the terminal

